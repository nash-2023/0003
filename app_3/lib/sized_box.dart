import 'package:flutter/material.dart';

class Sized_box extends StatelessWidget {
  const Sized_box({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 100.0,
      height: 100.0,
      child: Text(
        "Abdalla",
      ),
    );
  }
}
