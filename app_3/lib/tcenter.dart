import 'package:flutter/material.dart';

class Tcenter extends StatelessWidget {
  const Tcenter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        color: Colors.red,
        child: Text("how are you how are you how areu how are you "),
      ),
    );
  }
}
